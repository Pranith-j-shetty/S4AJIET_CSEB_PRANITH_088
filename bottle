 class Bottle{
    String brand;
    String color;
    int volume;
    int currentVolume=0;
    boolean isEmpty=true;
    boolean isFull=false;
    boolean isOpen=false;
    boolean isClosed=true;
    
    Bottle(String brand,String color,int volume){
        this.brand=brand;
        this.color=color;
        this.volume=volume;
    }
    void opencap(){
         System.out.println("Opening cap");
       this.isClosed=false;
        this.isOpen=true;
       
    }
    void closecap(){
        System.out.println("closing cap");
        this.isClosed=true;
        this.isOpen=false;
        
    }
    void drink(int consumed){
         System.out.println("Drinking"+consumed+"ml of water");
         this.currentVolume-=consumed;
         if(this.currentVolume<=0)
         this.isEmpty=true;
         if(this.currentVolume==this.volume){
             this.isFull=true;
         }
         if(volume>0){
      
        this.isEmpty=false;
    }
    }
    
    
    void fillwater(int volume){
        System.out.println("Filling"+volume+"ml of water");
        this.currentVolume+=volume;
        if(this.currentVolume==this.volume){
            this.isFull=true;
        }
        if(volume>0){
            this.isEmpty=false;
        }
    }
     public void display(){
         System.out.printf("%s,%s,%d,%d,%b,%b,%b,%b\n",this.brand,this.color,this.volume,this.currentVolume,this.isEmpty,this.isOpen,this.isFull,this.isClosed);
     }
    }
    class Main {
        public static void main(String args[])
        {
            Bottle Milton=new Bottle("milton","black",750);
            Milton.display();
            Milton.opencap();
            Milton.display();
            Milton.fillwater(100);
            Milton.display();
            Milton.drink(50);
            Milton.fillwater(750);
            Milton.display();
            Milton.drink(750);
            Milton.display();
            Milton.closecap();
            Milton.display();
            
            
        }
    }
    

 
